// Deprecated: previously complex hero component. Replaced by HeroSection.
// Keeping a no-op stub to avoid breaking stale imports that may linger.
export default function FeaturedHero() { return null; }
